node_modules
dist
.DS_Store
*.log

# Local secrets and deploy state. Only the empty template is tracked.
.env*
!.env.example
.vercel
.firebase

# Android signing keys (for the TWA wrapper later) must never be committed.
*.jks
*.keystore
*.p12
key.properties
